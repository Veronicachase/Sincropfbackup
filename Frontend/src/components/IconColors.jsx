import { Box } from "@mui/material";
import CircleIcon from '@mui/icons-material/Circle';


export default function IconColors(){
    <Box>
    if (project.status===iniciado) {
     <CircleIcon sx={{color:"#F2CB05"}}/>
    } if (project.status===terminado) {
     <CircleIcon sx={{color:"#8BB443"}}/>
    } else {
     <CircleIcon sx={{color:"#F25244"}}/>
    }
   </Box>
}